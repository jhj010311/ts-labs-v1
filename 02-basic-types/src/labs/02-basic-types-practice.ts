// [문제 2] 다양한 기본 타입 선언 연습
// (1) 아래 변수에 적절한 값을 대입하고, 그 타입이 무엇인지 주석으로 써보세요.

let height = 175; // number 타입
let message = "Hello, world!"; // string 타입
let passed = true; // boolean 타입
let zero = 0; // number 타입
let emptyStr = ""; // string 타입
let unset = undefined; // undefined 타입
let cleared = null; // null 타입

// (2) height, passed, message 각각에 어울리지 않는 값을 대입해보고, 타입 에러가 나는지 주석 해제해서 확인해보세요.
// height = "높음";
// passed = 1;
// message = false;
