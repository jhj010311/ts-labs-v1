{
    "compilerOptions": {
        "target": "ES6",
        "strict": true,
        "outDir": "dist",
        "moduleDetection": "force"
    },
    "include": ["src"]
}

//printLength(undefined);
// "strict": true 상태이면 에러가 나고, false이면 넘어간다

// "outDir": "폴더경로" 옵션 - ts파일을 컴파일할 때 다른 폴더로 분리
// "include": ["폴더경로"] 옵션 - 해당 폴더 내의 파일들만 타입스크립트 컴파일 대상으로 지정

// tsc 단독으로 실행시켜 전체를 컴파일시키는게 좋음

// "moduleDetection": "force" - 기본적으로 ts파일이 같은 폴더에 있으면 합쳐서 읽는 것을 각 파일별로 모듈화해서 방지하는 옵션
// 이걸 쓰지 않는 경우 개별 파일마다 export를 통해 일일이 모듈화해야함
